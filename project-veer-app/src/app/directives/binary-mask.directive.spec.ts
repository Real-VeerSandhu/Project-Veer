import { BinaryMaskDirective } from './binary-mask.directive';

describe('BinaryMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new BinaryMaskDirective;
    expect(directive).toBeTruthy();
  });
});
